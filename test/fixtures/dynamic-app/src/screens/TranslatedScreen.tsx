import { IconifyIcon } from "@huymobile/react-native-iconify";
import { t } from "../i18n";

export default function TranslatedScreen() {
  return <IconifyIcon name={t("common:back")} size={24} />;
}
